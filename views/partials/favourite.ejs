<form action="/favourites" method="POST">
  <button
    type="submit"
    class="bg-green-300 p-2 hover:bg-green-500 rounded-full border-2 border-green-800 transition duration-300 ease-in-out transform hover:scale-110 z-0"
  >
    Add to Favourite
  </button>
  <input type="hidden" name="id" value="<%= home._id %>" />
</form>
